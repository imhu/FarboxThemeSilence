extends base

block title
	post.title

block content
	div.post.detail_post
		h2.entrytitle
			a(href=post.url)
				post.title
		p.postmeta
			span(datetime=post.date.isoformat())= post.date.format('%B %d, %Y')
		.post_body= post.content
	.commentsresponsive	
		include include.comments
